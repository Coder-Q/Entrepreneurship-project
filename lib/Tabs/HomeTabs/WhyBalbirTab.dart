import 'package:flutter/material.dart';

class WhyBalbirTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Why Balbir'),
    );
  }
}
