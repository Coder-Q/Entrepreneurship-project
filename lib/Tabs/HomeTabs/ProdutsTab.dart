import 'package:flutter/material.dart';

class ProdutsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Product'),
    );
  }
}
